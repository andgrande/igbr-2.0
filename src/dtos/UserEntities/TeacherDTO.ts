export default interface TeacherDTO {
    ref: {},
    ts: number,
    data: {
        id: number,
        name: string,
        nickname: string,
        email: string,
        status: string,
    }
}